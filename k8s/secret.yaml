apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: todo-app
  labels:
    app: todo-app
type: Opaque
stringData:
  # Database Connection (UPDATE BEFORE DEPLOYMENT)
  DATABASE_URL: "postgresql://postgres:changeme@postgres:5432/todo_db"

  # OpenAI API Key (UPDATE BEFORE DEPLOYMENT)
  OPENAI_API_KEY: "sk-changeme"

  # JWT Secret (UPDATE BEFORE DEPLOYMENT)
  JWT_SECRET: "your-secret-key-change-this"

  # Cloudinary Configuration (Optional - UPDATE IF USING)
  CLOUDINARY_CLOUD_NAME: "changeme"
  CLOUDINARY_API_KEY: "changeme"
  CLOUDINARY_API_SECRET: "changeme"

# IMPORTANT: This file contains placeholder values.
# For production deployment:
# 1. Use external secret management (Sealed Secrets, External Secrets Operator, HashiCorp Vault)
# 2. Never commit actual secrets to version control
# 3. Add this file to .gitignore for production
